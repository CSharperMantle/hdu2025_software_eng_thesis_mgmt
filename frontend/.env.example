# Frontend Environment Variables

# API Base URL - points to the backend API
# Development: /api (relative path, recommended)
# Production: /api (works if frontend and backend are on same domain)
# Cross-origin: https://api.your-domain.com/api (if on different domain)
VITE_API_BASE_URL=/api
